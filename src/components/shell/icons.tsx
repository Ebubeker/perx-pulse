// The nav shell imports its icons from here; the canonical icon system now
// lives in @/components/Icon (lucide-backed). Re-export to keep call sites stable.
export { Icon, type IconName } from "@/components/Icon";
